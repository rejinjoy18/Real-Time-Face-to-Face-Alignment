# Real-Time-Face-to-Face-Alignment
Aligns facial landmarks of a user's face with that of a template face

To call the program, execute python3 main.py <style_image> and
follow on screen directions for alignment and matting. 

Make sure webcam is set up so user can roughly align his/her nose with that of the template style image to get a rough pose estimate.
